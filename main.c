#include "dlist.h"
#include "LIBFT/libft.h"

int main()
{
/*
	t_dlist *k;
    t_dlist *l;
    t_dlist *m;
    t_dlist *n;
    t_dlist *begin;

	k = ft_dlstnew(1);
    l = ft_dlstnew(2);
    m = ft_dlstnew(3);
    n = ft_dlstnew(4);
    begin = k;

   // ft_dlstadd_to_head(&n, m); // теперь эн указывает на начало списка, то есть эм
   // ft_dlstadd_to_head(&m, l);
   // ft_dlstadd_to_head(&l, k); // k is head


    ft_dlstadd_to_end(&k, &l); // k is always begin of the list
    ft_dlstadd_to_end(&k, &m);
    ft_dlstadd_to_end(&k, &n);
    while (k->next)
    {
        printf("%d", k->num);
        k = k->next;
    }
    printf("%s", "\n");
    while (k->previous)
    {
        printf("%d", k->num);
        k = k->previous;
    }

  //  printf("%d", n->num);
//    while (n->previous)
//    {
//        printf("%d", n->num);
//        n = n->previous;
//    }
	//printf("%d", k->num);

	*/

	printf("atoi:%d", ft_atoi("-2147483649"));
	return (0);
}